<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Home</title>
  </head>
  <body>
    <%- include('./partials/nav') %> <% if (locals.blogs) { %>

    <div class="container">
      <div class="row row-cols-4">
        <%blogs.forEach((blog,index) => { %>
        <div class="col">
          <%- include('./partials/blogCard' , { info : blog, }) %>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>
